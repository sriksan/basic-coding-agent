[project]
name = "basic-agent"
version = "0.1.0"
description = "Building basic ai agent"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "google-genai==1.12.1",
    "python-dotenv==1.1.0",
]
